# --- Telegram ---
aiogram>=3.13,<4
aiosqlite>=0.19
python-dotenv>=1.0

# --- Утилиты бота ---
matplotlib>=3.7          # графики прогресса
qrcode[pil]>=7.4         # QR-коды приглашений
requests>=2.31           # swarm_engine, embeddings (облачные API)
aiohttp-socks>=0.8       # работа через прокси (PROXY_URL)

# --- RAG: база знаний (эмбеддинги облачные — см. embeddings.py) ---
chromadb>=0.5

# --- Тесты ---
pytest>=7.4
