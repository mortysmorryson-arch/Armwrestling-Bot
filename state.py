"""
Состояния пользователей (FSM в памяти).
Вынесены отдельно, чтобы избежать циклических импортов между bot.py и роутерами.
"""

# Состояния напоминаний
# Ключи "YYYY-MM-DD_HH:MM" уже отработанных напоминаний (защита от повторной отправки).
# ВАЖНО: ключ на каждое ВРЕМЯ отдельно — раньше один флаг на день глушал все
# напоминания после первого срабатывания.
sent_reminders: set[str] = set()

# Состояния чата
chat_sessions: dict[int, int] = {}
send_workout_sessions: dict[int, int] = {}
awaiting_student_message: set[int] = set()

# Кэш для удаления упражнений
delete_exercise_cache: dict[str, str] = {}